# Development profile overrides
# Self-preservation is disabled in dev so that Eureka evicts instances quickly
# when they go down, making local development loops faster.
# In production, self-preservation defaults to true (Spring default),
# which protects against network partition false-positives.
eureka.server.enable-self-preservation=false
